//#1890DF

import 'package:flutter/material.dart';

final ThemeData applicationThemeData = ThemeData(
  brightness: Brightness.light,
  /* primaryColor: ApplicationColors.primaryColor,
  appBarTheme: AppBarTheme(
    color: Colors.white,
  ),
  textTheme: TextTheme(
    caption: TextStyle(color: ApplicationColors.textColor),
    button: TextStyle(color: ApplicationColors.textColor),
    overline: TextStyle(color: ApplicationColors.textColor),
  ), */
);

class ApplicationColors {
  //static const primaryColor = const Color(0xFF1890DF);
}